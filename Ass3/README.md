# Project 3 — the files to submit are in [`Final/`](Final/)

**Upload [`Final/project3_report.pdf`](Final/project3_report.pdf) and
[`Final/project3_code.zip`](Final/project3_code.zip). Nothing in this directory.**

Everything at this level is superseded. It predates Karan's 23 Aug retrain, so its numbers describe
a `best_model.pth` that was later overwritten — the report here claimed **0.8163**, and the honest
figure from the current artefacts is **0.8148**. Two files that used to sit here were removed
outright because they were the real trap: a `project3_report.pdf` with the old numbers has the
*exact* name the submission requires, is also 2 pages, and reads as correct. Git history still has
them.

What is left here is kept only as the record of what the draft looked like:

| | |
|---|---|
| `development.ipynb`, `main_report.ipynb` | pre-retrain; `development.ipynb` carried no stored output on the two cells that train the final model |
| `best_model.pth`, `histories.pkl` | the overwritten artefacts the 0.8163 figure came from |
| `project3_report.docx` | the draft report. See `Final/README.md` — the `.docx` cannot be submitted at all |
| `tutorial_reffered_development.ipynb` | the week-4 tutorial this project extends |

Read [`Final/README.md`](Final/README.md) before uploading anything.
